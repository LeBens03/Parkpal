package com.example.parkpal.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes

val Shapes = Shapes(
    small = RoundedCornerShape(RadiusSmall),   // e.g. chips, buttons
    medium = RoundedCornerShape(RadiusMedium), // e.g. cards, text fields
    large = RoundedCornerShape(RadiusLarge)    // e.g. bottom sheets, dialogs
)